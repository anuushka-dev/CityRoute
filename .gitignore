# Virtual environments
.venv/
venv/
env/

# Secrets / local env
.env

# Python caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Graph data (kept out of git except the committed reproducibility graph).
# NOTE: data/graphs/kanpur_central.graphml is intentionally committed so the repo and
# CI run out of the box. The larger legacy data/graphs/kanpur.graphml is NOT tracked.
data/graphs/*.graphml
!data/graphs/kanpur_central.graphml

# Local benchmark scratch / cache
cache/
benchmarks/*.tmp

# Local Phase 12 evidence output
.phase12_evidence/

# Local frontend environment
.env.local
